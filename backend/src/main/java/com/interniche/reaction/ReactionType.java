package com.interniche.reaction;

// Enum cho cot reac_type trong DDL — ENUM('like','dislike')
// @Enumerated(STRING) se luu ten enum dang chuoi trong DB
public enum ReactionType {
    like,
    dislike
}
